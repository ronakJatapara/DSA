function num(arr)
{
    let max= arr[0]
    let min=arr[0]
    let count =0;

    for(i=0;i<arr.length;i++)
    {
        if(arr[i]>max)
        {
            max=arr[i]
        }
        else if(arr[i]<min)
        {
            min=arr[i]
        }
    }

   let mid = aar.find(arr.min)
    

}



console.log(num([1,2,3,5,4]));
